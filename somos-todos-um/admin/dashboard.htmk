<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Administrativo - Somos Todos Um</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .header {
            background: white;
            padding: 20px 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }

        .header h1 {
            color: #333;
            font-size: 28px;
        }

        .btn-logout {
            background: #e74c3c;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background 0.3s;
        }

        .btn-logout:hover {
            background: #c0392b;
        }

        .welcome {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-bottom: 30px;
            text-align: center;
        }

        .welcome h2 {
            color: #333;
            margin-bottom: 10px;
        }

        .welcome p {
            color: #666;
            font-size: 16px;
        }

        .menu-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .menu-card {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
            cursor: pointer;
            transition: transform 0.3s, box-shadow 0.3s;
            text-decoration: none;
            color: inherit;
            display: block;
        }

        .menu-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
        }

        .menu-card .icon {
            font-size: 48px;
            margin-bottom: 15px;
        }

        .menu-card h3 {
            color: #333;
            font-size: 22px;
            margin-bottom: 10px;
        }

        .menu-card p {
            color: #666;
            font-size: 14px;
            line-height: 1.5;
        }

        .card-comissoes { border-top: 4px solid #3498db; }
        .card-igrejas { border-top: 4px solid #2ecc71; }
        .card-comprar { border-top: 4px solid #f39c12; }
        .card-comprovante { border-top: 4px solid #9b59b6; }
        .card-venda { border-top: 4px solid #e74c3c; }

        .footer {
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
            color: #666;
            font-size: 14px;
        }

        @media (max-width: 768px) {
            .header {
                flex-direction: column;
                gap: 15px;
                text-align: center;
            }

            .header h1 {
                font-size: 24px;
            }

            .menu-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>📊 Dashboard Administrativo</h1>
            <button class="btn-logout" onclick="logout()">Sair</button>
        </div>

        <!-- Welcome Section -->
        <div class="welcome">
            <h2>Bem-vindo ao Painel Administrativo</h2>
            <p>Selecione uma das opções abaixo para gerenciar o sistema</p>
        </div>

        <!-- Menu Grid -->
        <div class="menu-grid">
            <!-- Dashboard Comissões -->
            <a href="dashboard-comissoes.html" class="menu-card card-comissoes">
                <div class="icon">💰</div>
                <h3>Dashboard Comissões</h3>
                <p>Visualize e gerencie todas as comissões, relatórios financeiros e pagamentos</p>
            </a>

            <!-- Igrejas -->
            <a href="igrejas.html" class="menu-card card-igrejas">
                <div class="icon">⛪</div>
                <h3>Igrejas</h3>
                <p>Cadastre e gerencie igrejas parceiras, dados de contato e informações</p>
            </a>

            <!-- Comprar -->
            <a href="comprar.html" class="menu-card card-comprar">
                <div class="icon">🛒</div>
                <h3>Comprar</h3>
                <p>Realize compras, gerencie pedidos e acompanhe transações</p>
            </a>

            <!-- Comprovante -->
            <a href="comprovante.html" class="menu-card card-comprovante">
                <div class="icon">📄</div>
                <h3>Comprovante</h3>
                <p>Visualize e gerencie comprovantes de pagamento e transações</p>
            </a>

            <!-- Venda -->
            <a href="venda.html" class="menu-card card-venda">
                <div class="icon">💳</div>
                <h3>Venda</h3>
                <p>Registre vendas, acompanhe histórico e gerencie transações</p>
            </a>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>&copy; 2026 Somos Todos Um - Sistema Administrativo | Todos os direitos reservados</p>
        </div>
    </div>

    <script>
        // Função de logout
        function logout() {
            if (confirm('Tem certeza que deseja sair?')) {
                // Limpar dados de sessão (se houver)
                localStorage.removeItem('userSession');
                sessionStorage.clear();
                
                // Redirecionar para página de login
                window.location.href = 'admin.html';
            }
        }

        // Verificar se usuário está autenticado (opcional)
        window.addEventListener('DOMContentLoaded', function() {
            // Aqui você pode adicionar verificação de autenticação
            // Por exemplo, verificar se existe token no localStorage
            const userSession = localStorage.getItem('userSession');
            
            // Se não houver sessão, redirecionar para login
            // if (!userSession) {
            //     window.location.href = 'admin.html';
            // }
        });
    </script>
</body>
</html>
